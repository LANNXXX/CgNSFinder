# CgNSFinder

CgNSFinder 是用于谷氨酸棒杆菌（*Corynebacterium glutamicum*）基因组整合中性位点全基因组筛选的工具。

## 简介

本工具对基因组中所有基因依次施加以下 5 条过滤标准，识别适合外源基因整合的中性位点候选基因：

| 步骤 | 过滤标准                                                           |
| :--: | ------------------------------------------------------------------ |
|  1  | 基因注释为 `hypothetical protein`（假设蛋白）                    |
|  2  | 上游或下游至少一个相邻基因也是假设蛋白                             |
|  3  | 基因长度在 `[min_len, max_len]` bp 范围内                        |
|  4  | 侧翼区域具有足够长度的可用序列                                     |
|  5  | 两侧 `flank_len` bp 区域的 GC 含量在 `[gc_min%, gc_max%]` 之间 |

最终生成带颜色标注的 Excel 报告：

- 🟢 **绿色行**：通过全部过滤标准的候选位点
- 🟡 **黄色行**：未通过，含具体失败原因

## 环境要求

- Python ≥ 3.8
- 依赖包版本：`biopython 1.86`、`pandas 2.3.3`、`openpyxl 3.1.5`（及以上）

## 安装

```bash
# 克隆仓库
git clone https://github.com/LANNXXX/CgNSFinder.git
cd CgNSFinder

# 安装依赖
pip install -r requirements.txt
```

## 数据准备

本工具需要以下两个输入文件，均可从 NCBI Assembly 页面（登录号 [GCF_000011325.1](https://www.ncbi.nlm.nih.gov/datasets/genome/GCF_000011325.1/)）下载，下载后放入 `datas/` 目录：

| 文件                                      | 说明                       |
| ----------------------------------------- | -------------------------- |
| `GCF_000011325.1_ASM1132v1_genomic.fna` | 谷氨酸棒杆菌全基因组 FASTA |
| `cds_from_genomic.fna`                  | NCBI 格式 CDS 注释 FASTA   |

## 使用方法

### 默认参数运行

```bash
python filterData.py
```

### 自定义参数运行

```bash
python filterData.py \
    --cds_file datas/cds_from_genomic.fna \
    --fna_file datas/GCF_000011325.1_ASM1132v1_genomic.fna \
    --out_file my_report.xlsx \
    --min_len 600 --max_len 1200 \
    --gc_min 45 --gc_max 55
```

## 参数说明

| 参数            | 类型  | 默认值                                            | 说明                         |
| --------------- | ----- | ------------------------------------------------- | ---------------------------- |
| `--cds_file`  | str   | `./datas/cds_from_genomic.fna`                  | NCBI CDS 注释 FASTA 文件路径 |
| `--fna_file`  | str   | `./datas/GCF_000011325.1_ASM1132v1_genomic.fna` | 基因组序列 FASTA 文件路径    |
| `--out_file`  | str   | `genome_screening_report.xlsx`                  | 输出 Excel 报告路径          |
| `--min_len`   | int   | `500`                                           | 基因长度下限（bp）           |
| `--max_len`   | int   | `1500`                                          | 基因长度上限（bp）           |
| `--flank_len` | int   | `1000`                                          | 两侧 GC 分析区域长度（bp）   |
| `--gc_min`    | float | `40.0`                                          | 侧翼区 GC 含量下限（%）      |
| `--gc_max`    | float | `60.0`                                          | 侧翼区 GC 含量上限（%）      |

## 输出格式

Excel 报告 (`genome_screening_report.xlsx`) 包含以下列：

| 列名              | 说明                                  |
| ----------------- | ------------------------------------- |
| `seq_id`        | 染色体 / Contig 编号                  |
| `gene_id`       | Locus tag                             |
| `products`      | 蛋白质功能注释                        |
| `start`         | 基因起始位置（1-based）               |
| `end`           | 基因终止位置（1-based）               |
| `strand`        | 链方向（`+` / `-`）               |
| `length`        | 基因长度（bp）                        |
| `gc_left(%)`    | 左侧翼区 GC 含量（%）                 |
| `gc_right(%)`   | 右侧翼区 GC 含量（%）                 |
| `Filter_Status` | 过滤结果（`PASSED` 或具体失败原因） |
